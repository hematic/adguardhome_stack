services:
  adguardhome:
    image: adguard/adguardhome:latest
    container_name: adguardhome
    restart: unless-stopped
    network_mode: host
    volumes:
      - ${ROOT_PATH}/adguardhome/work:/opt/adguardhome/work
      - ${ROOT_PATH}/adguardhome/conf:/opt/adguardhome/conf
